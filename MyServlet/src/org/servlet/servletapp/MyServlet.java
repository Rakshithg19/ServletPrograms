package org.servlet.servletapp;

import java.io.IOException;
import javax.servlet.GenericServlet;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;

public class MyServlet extends HttpServlet{
	

	/*
	 * @Override
	 * 
	 * public void service(ServletRequest req, ServletResponse res) throws
	 * ServletException, IOException { Servlet String name = req.getParameter("nm");
	 * String emailid = req.getParameter("eml"); String password =
	 * req.getParameter("psw");
	 * 
	 * ServletOutputStream os = res.getOutputStream(); os.print("welcome" +name);
	 * 
	 * }
	 */
	

}

